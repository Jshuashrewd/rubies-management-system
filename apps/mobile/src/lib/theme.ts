// Ergonomic re-exports of the shared design tokens for use inside the app.
import { tokens } from "@rubies/shared";

export const { colors, spacing, radius, fontSize, fontWeight, fontFamily, roleBadge } =
  tokens;
export { tokens };
